# SSL Certificate Checker (Python)

A simple Python CLI tool that checks a website's SSL certificate and prints:
- Valid/Expired status
- Expiry date (UTC)
- Issuer organization
- Subject common name

## Requirements
- Python 3.10+

## Run
```bash
python src/cli.py
